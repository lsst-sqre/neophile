# Editable runtime dependencies (equivalent to install_requires)
# Add direct runtime dependencies here, as well as implicit dependencies
# with constrained versions.
#
# After editing, update requirements/main.txt by running:
#     make update-deps

GitPython
aiohttp
click
gidgethub
packaging
pydantic
ruamel.yaml
semver
xdg

# Temporary to prevent conflicts with requirements/dev.txt, which requires
# requests by way of sphinx.  requests pins idna to an older version.
requests
